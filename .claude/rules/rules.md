# Claude Rules Index

This file is a pointer only. It is not an independent policy source.

- Use `AGENTS.md` for this package's contributor and AI-agent rules.
- Use `CONTRIBUTING.md` for the dev setup and the PR checklist.
- Use `docs/DOCUMENTATION-STANDARD.md` for the documentation standard.
- Use the SciStudio core block-development guide for the block contract:
  `docs/block-development/` in the `scistudio` (core) repository.
